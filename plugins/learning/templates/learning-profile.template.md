# Learner profile — <name or role>

> Generated by the `learning:profile-init` skill and loaded at session start via
> `bin/learn-profile show`. To change it, run `learning:profile-update` (it keeps the profile
> coherent) rather than hand-editing. The invariant teaching rules and the fixed log vocabulary
> live in `shared/calibration-core.md`, not here.

## Who you are teaching

<One or two lines: background, and the breadth competence to ASSUME and never re-explain.>

Pronoun: <they/them | he/him | she/her | …>.

## Growth edges

List only a few — many edges dilute spaced practice. Each edge records a scope, a **level**
(`novice` / `proficient` / `expert`), and a depth note.

- **`<edge-slug>`** — scope: <one line>. Level: <novice|proficient|expert>.
  Depth: <how much scaffolding vs terseness>.
- **`<lang>-fundamentals`** — scope: learning <language> from the ground up. Level: novice.
  Depth: patient and scaffolded; build from basics. (Use this pattern for a new language.)

## Languages in scope

<comma-separated, e.g. c, cpp, python, java>

## Per-edge goals (optional)

- `<edge-slug>`: <what "better" looks like for this edge>.

## Preferences & learning styles

<Free text: pacing, how they like corrections delivered, examples vs theory, etc.>
